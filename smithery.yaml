build:
  dockerBuildPath: ./

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: "multipass-mcp",
      args: []
    })
